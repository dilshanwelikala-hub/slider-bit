[build]
  publish = "."
  functions = "netlify/functions"

# No redirects needed for the API: slider-save.mjs and slider-get.mjs each
# declare their own `path` (e.g. "/api/sliders/:id") via Netlify Functions v2's
# built-in path routing, so they're already reachable at those URLs directly.
